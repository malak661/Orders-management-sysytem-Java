package com.ordersystem.model;

import java.math.BigDecimal;
import java.time.LocalDateTime;
import java.util.List;

/**
 * Represents a customer order.
 * TODO: Add fields: id, customerId, items (List<OrderItem>), status (OrderStatus),
 *       discount (BigDecimal), tax (BigDecimal), subtotal, total, createdAt, updatedAt
 * TODO: Add constructors, getters, setters
 * TODO: Add helper methods if needed (e.g. calculateSubtotal - or keep this in OrderService)
 */
public class Order {

    // TODO: fields

    public Order() {
        // TODO
    }

    // TODO: getters and setters
}
